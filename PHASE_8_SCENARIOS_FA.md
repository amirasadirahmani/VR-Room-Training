# Phase 8 — Scenario & Challenge System

Phase 8 چهار سناریو را به همان ایستگاه VR اضافه می‌کند:

1. **نسخه دستی** — Handle در عقب دستگاه
2. **نسخه برقی** — Motor Unit به جای Handle
3. **اتصال سوسیس** — ترتیب جلویی صحیح: Plate → Lock Ring → Sausage Attachment
4. **چالش ۹۰ ثانیه‌ای** — ترتیب نسخه دستی، Hard difficulty، بدون راهنمای قرمز

## اصلاحات نهایی Phase 8

- Godot 4.7 parser warning در تایمر رفع شده است.
- پنل سناریو از جلوی Rack/پارتیشن خارج شده و در ناحیه قابل دسترس قرار گرفته است.
- Hover کل سطح دکمه را می‌گیرد و صدای Hover/Click غیرسه‌بعدی و واضح است.
- دکمه‌های سناریو به AssemblyManager متصل‌اند و Action واقعی اجرا می‌کنند.
- فقط یک دکمه در کل UI می‌تواند هم‌زمان Hover باشد؛ دکمه‌های مجاور دیگر با هم فعال نمی‌شوند.
- فاصله دکمه‌های سناریو بیشتر و حجم Hover باریک‌تر شده است.
- قانون تعامل حفظ شده: **GREEN = ACCEPTED**؛ با سبز شدن Auto-Snap همان لحظه شروع می‌شود و نیازی به رها کردن U/Grip نیست.
- در سناریوی سوسیس، مهره قفل قبل از سری سوسیس نصب می‌شود.

## Materialها

Overlayهای Phase 8 هیچ فایل داخل `assets/textures/workshop/` را جایگزین نمی‌کنند. بنابراین Poly Havenهایی که روی سیستم نصب کرده‌ای با اعمال این Overlay دست‌نخورده می‌مانند.

اسکریپت زیر فقط برای نصب/بازسازی Materialهاست و دیگر بخشی از روند عادی هر فاز نیست:

`./tools/download_polyhaven_cc0_1k.sh`

## تست نهایی

- پنل سناریو در سمت راست تخته و بدون مانع قابل دسترس باشد.
- روی هر دکمه فقط همان دکمه Hover شود، بزرگ شود و `[U]` نشان دهد.
- Click صدا داشته باشد و سناریو واقعاً عوض شود.
- Manual: فقط Handle فعال باشد.
- Electric: فقط Motor Unit فعال باشد.
- Sausage: ترتیب 8 مرحله‌ای باشد و Lock Ring قبل از Sausage Attachment نصب شود.
- Timed: زمان از 01:30 شمارش معکوس کند، guide قرمز مخفی باشد و سبز همچنان Auto-Snap قطعی باشد.
