# Phase 9 — Assessment & Final Training Polish (v2)

این فاز روی ارزیابی واقعی کارآموز و گزارش پایان جلسه تمرکز دارد و تمام اصلاحات نهایی Phase 8 را حفظ می‌کند.

## ارزیابی

در حالت Training و Challenge، پایان جلسه نتیجه آموزشی می‌دهد:

- **PASS / FAIL**
- Grade: **A / B / C / F**
- امتیاز نهایی
- Quality Score مستقل از Bonus زمان
- زمان
- Accuracy
- Mistakes
- First Try
- مرحله‌هایی که خطا در آن‌ها ثبت شده

حدنصاب پایه:

- Accuracy حداقل 75%
- Quality Score حداقل 70%
- تمام قطعات سناریو باید نصب شده باشند
- در Challenge باید قبل از پایان 90 ثانیه مونتاژ کامل شود

Grade A و B سخت‌گیرانه‌تر هستند. در Challenge برای A و B سرعت هم در نظر گرفته می‌شود.

## Accuracy منصفانه‌تر برای VR

نسخه اولیه Phase 9 از فرمول `completed / (completed + mistakes)` استفاده می‌کرد. این فرمول برای VR بیش از حد حساس بود، چون چند بار reposition یا drop می‌توانست Accuracy را شدیداً پایین بیاورد.

در v2، Accuracy مرحله‌محور است:

- 50% وزن برای Completion
- 50% وزن برای First Try

پس در یک سناریوی 7 مرحله‌ای که هر 7 مرحله کامل شده و 5 مرحله First Try بوده‌اند، Accuracy حدود 86% می‌شود؛ نه 37%.

## جلوگیری از شمارش خطای کاذب

دو منبع تورم Mistake اصلاح شده‌اند:

1. انتخاب قطعه اشتباه و سپس رهاکردن همان قطعه قبلاً می‌توانست دو خطا ثبت کند. حالا رهاکردن قطعه اشتباه خطای دوم ایجاد نمی‌کند.
2. رهاکردن قطعه صحیح در محل نامناسب در هر مرحله حداکثر یک بار Mistake حساب می‌شود؛ repositionهای بعدی خطای جدید نمی‌سازند.
3. گرفتن دوباره همان قطعه اشتباه در همان مرحله نیز فقط یک بار به‌عنوان خطای معنادار ثبت می‌شود.

Score و Error Breakdown همچنان خطاهای معنادار را نشان می‌دهند، اما حرکت‌های طبیعی دست در VR ارزیابی را خراب نمی‌کنند.

## رکوردها و Baseline

برای هر ترکیب **Scenario + Difficulty** رکورد جدا در `user://phase9_assessment_records.cfg` ذخیره می‌شود.

اولین اجرای کاملِ قابل ارزیابی، حتی در صورت Fail، به‌عنوان Baseline ذخیره می‌شود و Result Panel نشان می‌دهد:

`★ اولین رکورد ثبت شد`

Baseline شامل Score، Time، Accuracy، Grade و Pass/Fail است. بهترین رکورد رسمی همچنان فقط از بین اجراهای PASS انتخاب می‌شود:

- Best Score
- Best Time
- Best Accuracy
- Best Grade
- Attempts
- Passes

Saveهای نسخه قبلی Phase 9 به‌صورت خودکار migrate می‌شوند؛ اگر فقط Attemptهای Fail با فرمول قدیمی وجود داشته باشد و هیچ PASS ثبت نشده باشد، تاریخچه نامعتبر قدیمی برای این سناریو از Attempt 1 جدید شروع می‌شود و همان اجرا Baseline می‌سازد.

## تحلیل خطا

خطاها بر اساس مرحله‌ای که باید انجام می‌شد ثبت می‌شوند. مثال:

`تیغه×1، مهره×1`

این کمک می‌کند مشخص شود کارآموز دقیقاً در کدام مرحله مشکل داشته است، بدون اینکه dropهای تکراری عدد را غیرواقعی کنند.

## Free Practice

تمرین آزاد همچنان بدون امتیاز، Pass/Fail و رکورد رسمی است.

## Interaction / Mechanical Safety

تمام Fixهای Phase 8 حفظ شده‌اند:

- GREEN = Auto-Snap قطعی و فوری
- Hover انحصاری: فقط یک دکمه در هر لحظه
- صدای Hover و Click
- Scenario buttons واقعی و فعال
- ترتیب Sausage: `Plate → Lock Ring → Sausage Attachment`
- موقعیت قابل دسترس Scenario Panel
- پاک‌شدن Snap Clearance موقت هنگام Reset/تعویض سناریو

## Result Panel نهایی

- رکورد قبولی به‌صورت چندخطی نمایش داده می‌شود تا ترکیب فارسی با `Grade / Score / Time` به‌هم نریزد.
- Baseline و بهترین رکورد قبولی از هم جدا هستند.
- پیام `★ اولین رکورد ثبت شد` برای اولین اجرای معتبر نمایش داده می‌شود.

## Materialها

این Overlay هیچ فایلی از `assets/textures/workshop/` ندارد و Poly Haven را جایگزین نمی‌کند.

## تست Phase 9 v2

1. Manual Training را کامل کن و عدد Mistake را با خطاهایی که واقعاً انجام می‌دهی مقایسه کن.
2. یک قطعه صحیح را در همان مرحله چند بار جابه‌جا و Drop کن؛ باید فقط یک Drop Mistake ثبت شود.
3. یک قطعه اشتباه را بردار و رها کن؛ این تعامل نباید دو Mistake بسازد.
4. یک اجرای کامل با 5/7 First Try باید Accuracy حدود 86% بدهد.
5. اولین اجرای بعد از این hotfix، اگر Baseline قبلی وجود نداشته باشد، باید `★ اولین رکورد ثبت شد` نشان دهد.
6. Electric و Sausage را تست کن و مطمئن شو رکوردشان مستقل است.
7. Challenge را یک بار کامل و یک بار Timeout کن؛ Timeout باید FAIL بدهد.
8. Auto-Snap سبز و Hover تک‌دکمه‌ای را Regression Test کن.
