# Phase 4.2 — Room Boundary + Visible Workshop Materials

## اصلاح‌های این نسخه

- برخورد مجازی محیط برای Meta XR Simulator:
  - عبور از دیوار جلویی متوقف می‌شود.
  - عبور از دیوار چپ/راست متوقف می‌شود.
  - یک حد پشتی هم برای جلوگیری از خارج شدن از کف کارگاه فعال است.
- دلیل نیاز به RoomBoundary:
  - WASD در Meta XR Simulator مستقیماً Pose هدست شبیه‌سازی‌شده را جابه‌جا می‌کند؛ StaticBody معمولی نمی‌تواند خود Runtime را متوقف کند.
  - اسکریپت `res://scripts/xr/room_boundary.gd` موقعیت مجازی XROrigin را اصلاح می‌کند تا سر داخل کارگاه بماند.
- Collision mask بدن و دست‌ها حالا World را هم شامل می‌شود.
- Materialهای کارگاه واضح‌تر شده‌اند:
  - پنل‌های بتن روی دیوار
  - کف صنعتی تیره با درز و خط ایمنی
  - فلز رنگ‌شده صنعتی برای کمد/قفسه/در
  - برس‌خوردگی واضح‌تر برای میز و فریم‌ها
  - بافت تخته آموزشی
- Normal Mapها هنوز عمداً Dependency مستقیم ندارند تا خطای Load نسخه 4.1 برنگردد.

## تنظیم محدوده کارگاه

فایل:

`res://scripts/xr/room_boundary.gd`

مقادیر پیش‌فرض:

```gdscript
min_x = -2.95
max_x = 2.95
min_z = -3.02
max_z = 2.10
```

اگر بعداً ابعاد اتاق تغییر کرد همین چهار مقدار را تغییر بده.
